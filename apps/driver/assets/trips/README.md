# Driver trip list assets

`chevron.svg` is the exact Figma export of node `3:26` in driver list frame `3:2`.
`chevron.png` is its transparent 4× rasterization for React Native Image.
The list uses the frame's card styling for trip history while scheduling is disabled.
